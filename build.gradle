plugins {
    id 'java'
    id 'application'
}

group 'org.code.editor'
version '0.1.1-SNAPSHOT'

repositories {
    mavenCentral()
    mavenLocal()
//		flatDir {
//				dirs 'libs'
//			}
}

//tasks.withType(JavaCompile).all {
//    it.options.compilerArgs += ['--release', '8']
//}

dependencies {
    // testImplementation 'org.junit.jupiter:junit-jupiter-api:5.6.0'
    // testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine'
     implementation 'com.jetbrains.jsitter:jsitter:0.1-SNAPSHOT'
//    implementation name: 'jsitter-0.1-SNAPSHOT'
}

// test {
    // useJUnitPlatform()
// }

application{
    mainClass = 'Main'
}
